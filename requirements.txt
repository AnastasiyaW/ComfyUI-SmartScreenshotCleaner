# Core
torch>=2.0.0
numpy>=1.21.0
opencv-python>=4.5.0
Pillow>=9.0.0

# YOLO UI Detection (GPU accelerated)
ultralytics>=8.0.0
huggingface_hub>=0.16.0

# SOD - Salient Object Detection (GPU accelerated)
# For GPU: pip install onnxruntime-gpu
rembg>=2.0.50
onnxruntime-gpu>=1.15.0

# LaMa Inpainting (GPU accelerated, optional)
simple-lama-inpainting>=0.1.0
